<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tvc.supastriker"
    android:versionCode="3"
    android:versionName="1.2" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:launchMode="singleTask"
        android:theme="@style/AppTheme" >
        
        <activity
            android:name="com.tvc.supastriker.SupaStriker"
            android:configChanges="orientation|keyboardHidden|screenSize|smallestScreenSize"
            android:label="@string/app_name" >
        </activity>

        <!-- Splash screen activity -->
        <activity
            android:name="com.tvc.supastriker.SupaStrikerSplashScreen"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/title_activity_supa_striker_splash_screen"
            android:noHistory="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
          
          <!-- About screen activity -->
        <activity
            android:name="com.tvc.supastriker.About"
            android:label="@string/title_activity_about" 
            android:configChanges="orientation|keyboardHidden|screenSize|smallestScreenSize">
        </activity>
    </application>

</manifest>